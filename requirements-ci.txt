# Consumed by l9-lint-test / l9-analysis via `pip install -r requirements-ci.txt`.
# Editable install pulls runtime deps (pydantic ships PEP 561 types) so mypy
# strict does not treat BaseModel as Any under --ignore-missing-imports.
-e .
mypy>=2.3.1
types-requests
types-PyYAML
ruff>=0.16.3
pytest>=8
pytest-cov
